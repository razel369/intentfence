{
  "name": "intentfence",
  "version": "0.7.1",
  "description": "Add caller-observed x402 quote assessment and signed payment preflights to Gemini CLI agents.",
  "mcpServers": {
    "intentfence": {
      "command": "node",
      "args": [
        "${extensionPath}/mcp-stdio/bin/intentfence-gemini.mjs"
      ],
      "cwd": "${extensionPath}"
    }
  }
}
